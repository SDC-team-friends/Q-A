config:
  target: "http://localhost:3000/qa"
  phases:
    - duration: 30
      arrivalRate: 500

scenarios:
  - name: "get answers request"
    flow:
      - get:
          url: "/questions/30000/answers"